# Xipoo 隐私与安全审计及用户答复手册

## 可以对用户这样回答

“Xipoo 的课表、个人资料和社交数据按登录身份隔离，业务请求由云函数在服务端校验当前用户，不依赖前端传来的用户 ID。图片和文件使用云存储临时链接，应用不会公开永久下载地址。我们只在完成登录、课程和社交功能所需的范围内处理数据，并提供删除课程、资料和导入记录的能力。我们也会持续检查缓存、日志、权限和第三方 AI/OCR 服务，避免把敏感信息写入日志或暴露给其他用户。”

不要承诺“绝对不会泄露”或“完全匿名”；应说明隐私保护依赖云环境权限配置、第三方服务合规和用户设备安全。

## 当前代码审计结论

### 已具备的保护

- 多数业务云函数通过 `cloud.getWXContext().OPENID` 建立当前身份，再映射到内部 `xipooId`。
- 课表、活动日程、预约和个人资料查询通常带当前用户条件；预约取消也校验订单归属。
- 前端已经避免把云端失败结果当成真实数据，并对课表缓存做用户隔离。
- 图片访问主要通过 `getTempFileURL`，不是把永久公开 URL 写入页面。

### 必须由后端/云环境继续确认

1. CloudBase 数据库安全规则必须禁止客户端直接读取/写入 `users`、`schedules`、`friendships`、`reservation_orders` 等集合；所有写操作走云函数。
2. 每个云函数必须从 `OPENID` 推导用户，不得信任请求体中的 `userId/openid` 作为权限依据。
3. `friend.calculateAvailability` 等跨用户查询必须先验证双方共享授权，并只返回授权范围内的忙闲信息。
4. `getTempFileURL` 的有效期应尽量短；删除课程、头像、核验图时同步删除云文件。
5. 内容安全服务失败时当前部分代码会“放行”，生产环境应改为“待审核/拒绝”，不能把检测故障当作安全通过。
6. `ping` 或错误响应不应返回真实 `openid`、token、手机号、文件 ID 或内部堆栈。
7. OCR/AI 服务只发送完成识别所需的图片，并确认第三方服务的数据保留、训练使用和跨境处理政策。
8. 管理后台必须使用独立管理员身份、最小权限和服务端角色校验，不能只依赖前端隐藏按钮。

## 本次前端修复

- 移除登录成功时打印完整响应，避免 session/token、openid 等进入设备日志。
- 登录异常不再打印完整错误对象。
- 头像更新日志不再打印云文件 ID/临时地址。

## 发布前安全验收

- 用户 A 不能读取、修改或删除用户 B 的课程、预约、头像和 OCR 批次。
- 退出 A 后登录 B，设备缓存不显示 A 的课表或个人资料。
- 抓包只能看到短期请求凭证，不能获得永久文件下载地址。
- 删除数据后旧临时链接在有效期后失效，云存储对象同步清理。
- 关闭/删除账号后，缓存、云文件、OCR 任务和导出 ICS 一并处理。
- 生产日志不包含 token、openid、手机号、完整头像 URL、原图 base64 或课程详情。
- 对越权、重放、频繁请求、超大图片和恶意文本执行安全测试。

## 发生隐私事件时的答复原则

先确认影响范围和时间，再说明已采取的隔离、撤销链接、删除文件和通知措施；不要猜测、甩锅或声称“只是测试数据”。涉及个人信息时按适用法律和平台规则履行通知与留痕。
