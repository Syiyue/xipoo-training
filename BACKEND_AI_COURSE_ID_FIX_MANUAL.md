# 课程详情“点击后未找到”后端修复手册

## 现象

部分用户在课表点击课程可以看到课程卡片，但进入详情页后显示“课程未找到”。本次前端已经增加了 `id`、`courseId` 和旧缓存兼容；后端仍需从数据源彻底统一 ID，避免继续产生不完整记录。

## 根因

课程记录存在以下不一致：

- 新记录有业务 `id`；
- 旧记录可能只有 CloudBase `_id`；
- OCR/批量导入记录可能只有 `courseId` 或导入批次字段；
- ID 可能分别以数字和字符串保存。

课表列表、点击跳转和详情查询必须使用同一个稳定的业务 ID。

## 后端改法

1. 在课程写入入口（手动新增、OCR 批量导入、迁移脚本）统一生成 `id`。
2. `id` 必须是字符串，且同一条课程长期不变；更新课程不能重新生成。
3. 推荐优先级：已有合法 `id` → 可迁移的历史业务 ID → 基于课程唯一字段生成的稳定 ID。不要把 `Date.now()` 作为更新时的 ID。
4. 列表接口必须返回 `id`，不要返回数据库 `_id` 让前端自行猜测。
5. 详情接口/列表查询统一按字符串比较，并兼容历史 `courseId` 的一次性迁移。
6. 批量导入时，为每条主课程保存稳定 `id`；同一课程跨周只能合并周次，不能每周生成新 ID。

## 推荐迁移逻辑（伪代码）

```js
function ensureCourseId(record) {
  if (record.id) return String(record.id)
  if (record.courseId) return String(record.courseId)
  // 使用可复现字段，不要使用当前时间
  return [record.courseCode || record.title || 'course', record.section || '',
    record.weekday || '', record.start || '', record.end || ''].join('|')
}
```

迁移完成后，建议为 `userId + id` 建立唯一约束或在写入前做幂等检查。

## 验收清单

- 新增课程返回 `id`；
- OCR 课程返回 `id` 且跨周更新不变；
- 旧数据只有 `_id` 时，列表接口仍提供稳定业务 `id`；
- 点击课表卡片传入的 ID 可在详情查询中找到；
- 同一用户同一课程不会因刷新、重复导入产生多个 ID；
- 数字/字符串 ID 混用不会导致查询失败；
- 覆盖手动课程、OCR 课程、周期课程和历史迁移数据的接口测试。

## 注意

本手册只描述后端 AI 应执行的修复方案。本次代码变更不直接修改后端实现。
