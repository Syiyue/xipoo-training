# 小程序数据一致性产品质询与修复清单

## 必须回答的问题

- 页面展示的数据是否明确属于当前登录用户？
- 缓存是否有用户隔离、时间戳和失效策略？
- 预拉取结果是否可能覆盖用户刚刚新增或修改的数据？
- 云端请求失败时，是否会把 mock 或旧快照伪装成最新数据？
- 同一课程是否始终使用稳定 ID，列表、编辑、详情、删除是否一致？
- 多次快速进入页面时，旧请求返回是否可能覆盖新请求？

## 本次已修复

- 预拉取只用于首屏加速，不能跳过实时云端刷新。
- 本地课表缓存增加 `ownerId` 校验；没有用户归属的旧缓存不再使用。
- 写入缓存时必须绑定当前用户 ID，避免跨用户串数据。
- 云端成功结果无论是否改变页面快照，都会更新本地缓存。

## 发布前验收

1. 用户 A 新增课程后立即切换页面，仍显示新课程。
2. 用户 A 退出并登录用户 B，不能看到 A 的缓存课程。
3. 断网进入页面时只能看到明确标记的旧快照，不能伪装为实时数据。
4. 恢复网络后重新进入页面，最终结果必须与 `listSchedules` 完全一致。
5. 快速连续进入/退出页面，旧请求不能覆盖最新请求。
6. 同一课程从列表点击详情、编辑、删除，使用同一稳定 ID。
